# เส้นทางการจูนระบบจำแนกเครื่องมือผ่าตัด — ตั้งแต่ Baseline จนถึง CAHM

> เขียนให้คนทั่วไปอ่านเข้าใจ — ไม่ต้องมีพื้นหลัง AI ก็เห็นภาพว่าทำอะไรไปบ้างและทำไม

---

## 1) โจทย์ที่เจอ

- **งาน:** แยกเครื่องมือผ่าตัด 14 แบบ ที่วางบน **ผ้าสีเขียว**
- **รูปถ่าย:** กล้องติดตายตัว ระยะเท่าเดิมทุกครั้ง
- **ปัญหาใหญ่:** ไม่ใช่สีถาด แต่เป็น **เงา** — แสงทำให้เกิดเงาบนผ้า ทำให้หาขอบเครื่องมือได้ยาก
- **ปัญหาย่อย:** บาง class หน้าตาเหมือนกันมาก ต่างกันแค่ **ความยาว** นิดเดียว (เช่น `Needle_Holder` กับ `Artery_Forceps`)
- **ข้อมูลน้อย:** มีแค่ ~400 รูป เฉลี่ย 30 รูปต่อ class — ถ้าโมเดลใหญ่เกินจะจำข้อสอบได้แต่ทำข้อสอบใหม่ไม่ได้ (overfit)

---

## 2) Baseline — ระบบตั้งต้นที่ทำให้รันได้ก่อน

### 2.1 ทำไมเลือก DINOv2 + LoRA

- **DINOv2-S** คือโมเดลที่เรียนจากรูปนับล้านรูปมาก่อนแล้ว มองรูปเก่งตั้งแต่เกิด ไม่ต้องสอนใหม่ทั้งหมด
- เราเลือกตัวเล็ก `ViT-S/14` (21 ล้านพารามิเตอร์) เพราะข้อมูลน้อย — ตัวใหญ่จะจำเก่งเกิน
- **LoRA (r=16)** คือวิธีจูนแบบประหยัด — แทนที่จะแก้ทั้งโมเดล เราแปะแผ่นเล็กๆ บนจุดสำคัญ (`query/value` ของ attention) ให้เรียนเพิ่มแค่ ~1.5 ล้านตัว

### 2.2 ทำไมต้องวัดความยาว

- ปกติเราย่อรูปเป็น `504×504` เพื่อให้โมเดลรับได้ แต่การย่อทำให้ **ขนาดจริงหาย** — เครื่องมือที่ยาวต่างกัน 2 ซม. พอย่อแล้วดูเท่ากัน
- เราเลยวัดความยาวจริงจาก **mask** ที่วาดไว้แล้ว: เอา `minAreaRect` ครอบเครื่องมือ → เอาด้านยาวที่สุด → ได้ `length_px`
- แล้วทำ `length_norm = (length - ค่าเฉลี่ย_train) / ส่วนเบี่ยงเบน` เพื่อให้ทุก class เทียบกันได้

### 2.3 เอาภาพ + ความยาวมารวมกัน

```
ภาพ → DINOv2 → เวกเตอร์ 384 มิติ ─┐
                                  ├─ ต่อกัน (385) → Linear → 384 มิติ → ส่งให้ Loss
ความยาว 1 ค่า ────────────────────┘
```

### 2.4 Loss แบบ ArcFace

- ปกติโมเดลจะบอกว่า “รูปนี้เหมือน class ไหน” ด้วยระยะห่าง
- ArcFace บอกว่า “ต้องห่างกันอย่างน้อย 28.6 องศา” — บังคับให้ class ที่หน้าตาคล้ายกันถูกดันให้ห่างกันมากขึ้น จำได้แม่นขึ้น

### 2.5 ผล Baseline

- `1032` รูปเทรน / `244` รูปเทส
- **Accuracy 0.9467** — เก่งแล้ว แต่พลาดกระจุกเดียว: `Needle_Holder ↔ Artery_Forceps` สับสนกัน `5+3 = 8` ครั้งจาก 11 ครั้งที่พลาดทั้งหมด
- แปลว่า จุดอ่อนชัดเจน — ไม่ใช่ทั้งระบบห่วย แต่คู่เดียวที่เหมือนกันเกิน

---

## 3) การจูน — หาขนาดภาพที่ดีที่สุดก่อนเทรนจริง (kNN Probe)

ก่อนเสียเวลาเทรน 20-30 นาที เราเช็คด้วยวิธีเร็ว `kNN Probe`:

1. เอา DINOv2 ที่ยังไม่ได้จูนเลย ดึง feature ดิบ `CLS token` ของทุกภาพ
2. ให้ `1-Nearest Neighbor` ทายว่า val แต่ละรูปใกล้ train รูปไหนที่สุด
3. ได้ `kNN accuracy` — ถ้าสูง แปลว่า feature ดิบก็แยก class ได้แล้ว เทรนต่อคุ้ม

**ลองหลายขนาด (หาร 14 ลงตัว, bbox ขยาย 0.15):**

| ขนาดภาพ | kNN | หมายเหตุ |
|---|---|---|
| 224 | 0.7131 | เล็กไป รายละเอียดปลายคีมเหลือ ~20px |
| 504 | **0.7582** | **ดีสุด — เลือกเป็น default** |
| 518 | 0.7459 | native ของ DINOv2 แต่ยังแพ้ 504 |
| 616 | 0.7295 | ใหญ่เกิน pos-embed ต้องยืดเยอะ |

**ลอง bbox ขยาย ที่ 504:**

| ขยาย | kNN |
|---|---|
| 0.0 (ไม่ crop) | 0.3074 | ภาพเต็มมี 2 ชิ้นต่อภาพ label กำกวม |
| 0.10 | 0.7377 |
| **0.15** | **0.7582** |
| 0.20 | 0.7336 |

สรุป: เลยล็อค `504` + `bbox 0.15` เป็นค่าเริ่มต้น — ไม่ได้สุ่ม แต่มาจากการวัดก่อนเทรน

---

## 4) ส่วนเสริมที่ลอง — ทำไปทำไม แล้วเก็บอะไรไว้

Baseline วิ่งได้แล้ว แต่เรารู้ว่าคู่ `Needle ↔ Artery` คือคอขวด เลยลอง 3 ไอเดีย แล้ววัดแบบ `ablation` (ลองทีละตัวแล้วเทียบ)

### 4.1 CAHM — ให้โมเดลโฟกัสคู่ที่พลาดบ่อย (เก็บไว้)

**ทำไปทำไม:** แทนที่จะให้ทุก class สำคัญเท่ากัน ให้ `Needle↔Artery` หนักขึ้นเมื่อมันพลาดบ่อย

**ทำยังไง (เข้าใจง่าย):**
```
ทุก epoch ดูตารางสับสน 14×14
คู่ไหนสับสนเยอะ → คะแนนยาก d(i,j) สูง
คะแนนนี้ค่อยๆ เฉลี่ยแบบ EMA (จำอดีต 90% + ปัจจุบัน 10% กันกระโดด)
แต่ละรูปที่ label = y จะได้ weight w = 1 + 2.0 * (คะแนนยากสูงสุดของ y)
loss ของรูปนั้น = loss เดิม * w   # เริ่มใช้หลัง epoch 10 ให้โมเดลนิ่งก่อน
```

**ผล:** `0.9467 (8) → 0.9590 (3)` — `Needle` ลดจาก 8 ครั้งเหลือ 3 ครั้ง ดีสุดในทุกสูตร **จึงเก็บไว้และเปิดเป็น default**

### 4.2 LGMS — เพิ่ม margin ตามความยาว (ลองแล้วตัดออก)

**คิดว่าจะช่วย:** คู่ที่ยาวใกล้กันควรถูกดันให้ห่างกว่าเดิม

**ทำยังไง:** หาคู่ที่ยาวใกล้กันสุด 2 คู่ แล้วเพิ่ม margin `28.6° + 10°*ความใกล้` ให้ class นั้น

**ผล:** `0.9385 (9)` — แย่กว่า baseline เลยตัดออก (อาจเพราะความยาวในชุดนี้กระจายไม่พอให้ margin ช่วย)

### 4.3 SEF — เพิ่ม branch ดูขอบด้วย Scharr (ลองแล้วตัดออก)

**คิดว่าจะช่วย:** ผ้าเขียว + โลหะ + เงา = contrast ต่ำ อยากให้มี branch ดูขอบโดยตรง

**ทำยังไง:** ทำภาพเทา → Scharr หาขอบ → CNN 3 ชั้น → 64 มิติ → ต่อกับ 384+1 เป็น 449 → ย่อกลับ 384

**ผล:** `0.9549 (7)` — ดีขึ้นเล็กน้อย แต่ไม่เท่า CAHM และ `all` (0.9508) ก็ไม่ชนะ CAHM เดี่ยว เลยตัดออกเพื่อไม่ให้ระบบซับซ้อนเกิน

**สรุป ablation 6 สูตร (504, 1032/244):**

| สูตร | Val Acc | Needle↔Artery | เก็บไหม |
|---|---|---|---|
| baseline | 0.9467 | 8 |  |
| **cahm** | **0.9590** | **3** | **เก็บ** |
| lgms | 0.9385 | 9 | ตัด |
| sef | 0.9549 | 7 | ตัด |
| cahm+lgms | 0.9508 | 6 | ตัด |
| all | 0.9508 | 5 | ตัด |

---

## 5) ระบบสุดท้ายที่ส่งมอบ

- **Config เริ่มต้น:** `504`, `bbox 0.15`, `batch 32` (T4) / `16` (1650 4GB), `lora r=16`, `use_cahm=True`
- **Notebook:** `DentalInstrument_DINOv2_ArcFace.ipynb` — `%%writefile` ครบ 6 ไฟล์ → `3.5` probe → `4` train (CAHM) → `5` evaluate
- **วิธีรัน:** อัปโหลด `dataset.zip` (แตกได้ `dataset/train, valid`) → `Runtime T4` → `Run all` → `probe ~0.75` → เทรน ~25 epoch → `0.9590`

> ทั้งหมดนี้ทำโดยยึดหลัก: วัดก่อนเทรน (kNN), แก้จุดที่พลาดชัดที่สุด (Needle), ลองหลายไอเดียแล้วเก็บแค่ตัวที่พิสูจน์ว่าได้ผลจริง
